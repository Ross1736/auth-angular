import { Component } from '@angular/core';
import { RouterModule } from '@angular/router';

@Component({
  selector: 'app-layout-protected',
  imports: [RouterModule],
  templateUrl: './layout-protected.html',
  styleUrl: './layout-protected.scss',
})
export class LayoutProtected {}
